export interface Menu {
  title: string;
  to: string;
}

export interface FeaturedArtist {
  name: string;
  imageUrl: string;
  link?: string;
}

export interface Partner {
  logo: string;
  name: string;
  url?: string;
}
